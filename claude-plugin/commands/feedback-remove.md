---
description: Unwire the claude-feedback Vite plugin from the current project.
allowed-tools: Bash(node:*)
---

Below is the unwiring script's raw output — the user does not see it, relay
the outcome in one line.

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/unwire.mjs" 2>&1 || true`
