---
description: Wire the claude-feedback Vite plugin into the current Vue+Vite project.
allowed-tools: Bash(node:*)
---

Below is the wiring script's raw output — the user does not see it, relay the
outcome in one line (e.g. "wired" / "updated vX -> vY" / "already up to date" /
why it was skipped).

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/wire.mjs" --update 2>&1 || true`
